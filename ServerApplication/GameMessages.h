#pragma once

enum EGameMessages
{
	ID_SERVER_TEXT_MESSAGE = ID_USER_PACKET_ENUM + 1, 
	ID_CLIENT_TEXT_MESSAGE = ID_USER_PACKET_ENUM + 2,
	ID_SERVER_CHAT_MESSAGE = ID_USER_PACKET_ENUM + 3,
	ID_CLIENT_CHAT_MESSAGE = ID_USER_PACKET_ENUM + 4,
	ID_CHAT_DUMP = ID_USER_PACKET_ENUM + 5
};